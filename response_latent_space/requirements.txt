numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2
scikit-learn>=1.4,<2
umap-learn>=0.5.5,<0.6
matplotlib>=3.8,<4
pyyaml>=6,<7
seaborn>=0.13,<0.14

# Optional Parquet support:
# - If you need to read/write `.parquet`, install ONE of:
#   - pyarrow
#   - fastparquet
# On very new Python versions, wheels may not be available yet and source builds can fail.
